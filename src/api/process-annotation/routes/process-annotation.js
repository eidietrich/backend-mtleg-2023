'use strict';

/**
 * process-annotation router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::process-annotation.process-annotation');
